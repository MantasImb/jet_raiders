meta {
  name: 00 Create Player D Session
  type: http
  seq: 4
}

post {
  url: {{process.env.HEAD_URL}}/guest/init
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "display_name": "Bruno_Player_D"
  }
}

tests {
  test("creates player D guest session", function() {
    const body = res.getBody();

    expect(res.getStatus()).to.equal(200);
    expect(body.guest_id).to.be.a("string");
    expect(body.session_token).to.be.a("string");

    bru.setVar("player_d_guest_id", body.guest_id);
    bru.setVar("player_d_session_token", body.session_token);
  });
}
