meta {
  name: 14 Invalid Region Unknown Returns 400
  type: http
  seq: 25
}

post {
  url: {{process.env.HEAD_URL}}/matchmaking/queue
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "session_token": "{{player_d_session_token}}",
    "player_skill": 1300,
    "region": "ap-south"
  }
}

tests {
  test("exact-match validation rejects unknown concrete regions", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
